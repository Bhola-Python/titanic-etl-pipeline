# titanic-etl-pipeline
This project loads the Titanic dataset, cleans missing values, transforms the data using PySpark DataFrame API, and finally loads the cleaned dataset into MySQL using JDBC connector
